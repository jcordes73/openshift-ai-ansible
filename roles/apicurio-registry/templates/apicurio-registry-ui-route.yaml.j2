apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: apicurio-ui-service
  namespace: apicurio-registry
  annotations:
    router.openshift.io/service-ca-certificate: "true"
spec:
  host: registry-ui.{{ openshift_apps_domain }}
  to:
    kind: Service
    name: apicurio-ui-service
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect

